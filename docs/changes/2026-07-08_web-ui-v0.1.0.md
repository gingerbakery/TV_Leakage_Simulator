## 2026-07-08: Web UI v0.1.0 + 고급 입력 확장

### 주요 변경
- `run_web.py` 추가 (내장 stdlib 웹서버, 웹 폼 기반 시뮬레이션 실행)
- 고급 입력 확장
  - Gap: nominal/sigma/transmissive threshold/ROI face indices
  - Emitter: face/volume_box/volume_sphere + strength/mode/distribution/위치 파라미터
  - Material: preset 선택 + 반사율/확산/스펙큘러/roughness/absorption/alpha 개별 override
- 실행 결과 링크 제공: `JSON`, `CSV`, `heatmap`, `report`
- `run_gui.py` 고급 입력 필드 강화
- 엔진 실행 API(`execute_run`) 파라미터 고도화
  - `emitters`, `replace_emitters`, `material_preset_id`, `material_override`

### 체크 포인트
- 아직 파일 업로드는 미지원이며 `CAD path` 직접 입력 기반
- 웹 UI 라우팅은 단일 프로세스 동기 실행(동시 요청 다수는 실무 테스트에선 큐레이션 필요)
