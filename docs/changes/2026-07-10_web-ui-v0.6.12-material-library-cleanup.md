# Web UI v0.6.12 - Material library cleanup

## Summary
- 왼쪽 `Material library`를 더 간결한 접이식 트리 구조로 정리했다.
- `Base materials`, `Surface properties`, `BSDF assets`, `Saved optical profiles`, `Assignments`를 각각 드롭다운 섹션으로 분리했다.
- `New material`, `New surface property`, `New BSDF` 버튼을 추가하고 `입력 -> Save` 흐름의 등록 폼을 붙였다.

## Updated files
- `run_web.py`

## Key UI changes
- 하위 항목 제목/설명 색 대비 개선
- 리스트를 상위 메뉴처럼 접이식 구조로 변경
- 재질/표면/BSDF 신규 등록을 개별 폼으로 분리
- 기존 장문 설명을 줄이고 짧은 서브라벨만 유지

## Notes
- 현재 등록 동작은 UI mockup 단계이며 브라우저 메모리 상태에 저장된다.
- 이후 단계에서 실제 프로젝트 설정 파일 저장과 연결 가능하다.
